<system-interrupt reason="tool_progress_finalize_required">
The prior redirect was ignored and {{noEvidenceCount}} additional observation actions produced no new evidence. This provider turn cannot call tools.

Return a concise conclusion for the active user request from the evidence already collected. Separate confirmed facts, inference, and missing evidence. If the request required implementation and successful mutation evidence is {{mutationCount}}, do not claim that files were changed. If verification evidence is incomplete, state exactly what remains unverified. A real user message supersedes this instruction.
</system-interrupt>
