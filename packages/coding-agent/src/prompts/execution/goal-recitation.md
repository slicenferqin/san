[Objective recitation — host-pinned goal for this turn]

Goal: {{objective}}
{{#if todoLines.length}}
Progress:{{#each todoLines}} {{this}};{{/each}}
{{/if}}

This turn has run many tool calls. Re-check: are your recent actions still serving this goal? If you have drifted, correct course now. If the goal is already met, verify and conclude instead of continuing to act.
